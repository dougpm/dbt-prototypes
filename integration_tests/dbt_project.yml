
name: 'playground_integration_tests'
version: '1.0.0'
config-version: 2

profile: 'playground'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  +schema: 'RAW_MODEL_1'


